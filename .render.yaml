services:
  - type: web
    name: call-insights-app
    env: python
    plan: free
    buildCommand: ./build.sh && pip install -r requirements.txt
    startCommand: streamlit run main.py --server.port=$PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
